projection;
strict ( 2 );

define behavior for ZC_MAT_HEADER alias Material
{

  use create;
  use update;
  use delete;

  use association _Items { create; }
}

define behavior for ZC_MAT_ITEMS alias Item
{

  use update;
  use delete;

  use association _Material;
}